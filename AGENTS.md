# 한울 프로젝트 작업 규칙

이 파일은 한울 배송 프로젝트에서 Codex, Claude, 기타 자동화 에이전트가 작업할 때 반드시 먼저 확인해야 하는 프로젝트 전용 규칙이다.

## 필수 언어

- 모든 설명과 보고는 한국어로 작성한다.
- 코드 식별자, 명령어, API 경로, DB 컬럼명은 원문 그대로 사용한다.

## 필수 참조 문서

작업 전에 아래 문서를 먼저 확인한다.

1. `CLAUDE.md`
2. `docs/ADDRESS_STRATEGY.md`
3. `docs/ADDRESS_TESTING_GUIDE.md`

주소, 배송동, 지도, 순번, 배차 관련 작업은 `docs/ADDRESS_STRATEGY.md`를 최우선 기준으로 삼는다.

## 주소/DB 핵심 규칙

- 행안부 원본 주소 DB는 `nexus_address` 스키마를 기준으로 사용한다.
- 주소는 문자열만 저장하지 않는다. 주문에는 행안부 표준키와 매칭 상태를 함께 저장한다.
- 주소 검증은 백엔드 `address_resolver.py`를 통해 수행한다.
- 프론트와 모바일은 주소 문자열을 직접 해석하지 않는다.
- 주소 검증 API는 `/api/v1/addresses/resolve`와 `/api/v1/addresses/resolve-batch`를 사용한다.
- 주문 생성/수정/배차 시 주소 검증 결과를 `orders`와 `address_resolution_logs`에 남긴다.
- 반복 실패 주소는 `address_overrides`에 현장 보정 규칙으로 저장한다.
- 주소 캐시는 `address_cache`를 사용하며, `normalized_query`, 표준키, 매칭상태, 좌표 출처를 함께 보관한다.

## 배송동 규칙

배송 가능 구역은 광주시 18개 동이다(면·읍 및 능평동·신현동 제외).

- 핵심 4개 동: 경안동, 송정동, 쌍령동, 탄벌동
- 추가 14개 동: 고산동, 매산동, 목동, 목현동, 문형동, 삼동, 양벌동, 역동, 장지동, 중대동, 직동, 추자동, 태전동, 회덕동

배송 허용동 단일 소스: 백엔드 `address_resolver.SERVICE_DONGS`, 프론트 `@/lib/utils`의 `DONG_LIST`.
배송 구역 정책은 코드 상수보다 `delivery_zones` DB를 우선한다. 법정동과 실제 배송 구역이 다르면 `address_overrides.force_service_dong`을 우선한다.

## 배차/순번 규칙

- 배차 실행 결과는 `dispatch_runs`, `dispatch_run_items`에 반드시 남긴다.
- 40건 초과 시 총관리자에게 기사 추가/분배 판단을 요청한다.
- 60건 이상은 1명 처리 또는 2명 이상 분배 여부를 총관리자가 판단한다.
- 순번은 배송동, 도로명, 건물번호, 좌표 기반 최적화 결과를 함께 고려한다.
- 기사가 모바일에서 수동으로 순번을 바꾸면 `sequence_source = manual`로 저장한다.

### 기사 수별 추천 배차 구성 규칙

- 1명 배차: 모든 오늘 배송 주문을 선택 기사 1명에게 배정하고, 기사 내부 배송순번은 거리 기반 최적화로 재계산한다.
- 2명 배차: 관리자 화면에서 18개 동 추천 구성을 미리 선택해 보여주고, 선택된 `dong_groups`를 그대로 배차 API에 전달한다.
  - 1기사 권역: 경안동, 송정동, 쌍령동, 탄벌동, 역동, 장지동, 태전동, 회덕동, 양벌동
  - 2기사 권역: 고산동, 매산동, 목동, 목현동, 문형동, 삼동, 중대동, 직동, 추자동
- 3명 배차: 중앙/동측/서남외곽 3권역으로 자동 추천한다.
  - 1기사 권역: 경안동, 송정동, 역동, 장지동, 탄벌동, 회덕동
  - 2기사 권역: 쌍령동, 양벌동, 태전동, 고산동, 중대동, 직동
  - 3기사 권역: 매산동, 목동, 목현동, 문형동, 삼동, 추자동
- 4명 배차: 중앙/동북/남동/서남 4권역으로 자동 추천한다.
  - 1기사 권역: 경안동, 송정동, 역동, 장지동
  - 2기사 권역: 탄벌동, 회덕동, 쌍령동, 양벌동, 태전동
  - 3기사 권역: 고산동, 중대동, 직동, 삼동
  - 4기사 권역: 매산동, 목동, 목현동, 문형동, 추자동
- 2~4명 배차 화면에서는 추천 동 구성을 먼저 표시하고, 관리자가 동 칩을 눌러 기사 그룹을 조정할 수 있어야 한다.
- 추천 구성은 빠른 배차를 위한 기본값이며, 총관리자의 현장 판단으로 변경 가능하다.
- 어떤 기사 수에서도 주문 누락은 허용하지 않는다. 추천 그룹에 없는 동의 주문이 있으면 가장 적은 주문을 가진 기사에게 자동 보정 배정한다.
- 배차 후 각 기사 그룹 내부 배송순번은 Kakao Mobility 실제 도로거리 행렬을 우선 사용해 최적화한다. Kakao Matrix 호출이 불가하면 좌표 기반 최적화로 fallback한다.
- `sequence_source`는 Kakao Matrix 성공 시 `kakao_matrix`, 자동 fallback 시 `auto`, 기사 수동 변경 시 `manual`로 저장한다.
- 1명 배차는 전체 주문을 한 경로로 최적화하므로 순번은 작동하지만, 18개 동 전체를 한 명이 담당하면 긴 점프가 생길 수 있다. 물량과 권역이 넓으면 2~4명 추천 구성을 우선 검토한다.

## 개인정보/보안 규칙

- 성명, 전화번호, 주소, 상세주소, 생년월일은 개인정보로 취급한다.
- 개인정보 노출 로그를 남기지 않는다.
- 주소 검증 로그와 수동 보정 기능은 관리자 이상 권한으로 제한한다.
- 파일 업로드, 인증, 권한 변경 작업은 기존 보안 패턴을 유지한다.

## 작업 완료 규칙

형님이 명시한 운영 방식에 따라 실제 코드 작업 후에는 다음 순서로 마무리한다.

1. 로컬 검증
2. 빌드
3. 커밋
4. 푸시
5. 서버 배포
6. 헬스체크 및 마이그레이션 확인

배포 명령:

```bash
gcloud compute ssh hanwool-server --project=hanwool-delivery-2026 --zone=asia-northeast3-a --command="cd /opt/hanwool && sudo git pull && sudo docker compose up -d --build"
```

## 기존 작업 보호

- 이미 완료된 기능과 디자인을 임의로 삭제하거나 되돌리지 않는다.
- 사용자가 만든 변경사항을 자동으로 되돌리지 않는다.
- DB 마이그레이션은 기존 revision을 수정하지 말고 새 revision으로 추가한다.
- 불필요한 대규모 리팩터링보다 요청 범위 안에서 안전하게 확장한다.
