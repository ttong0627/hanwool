# 경안시장 집배송 프로젝트 — CLAUDE 지침

> 글로벌 규칙(~/.claude/CLAUDE.md)이 항상 우선 적용됩니다.
> 이 파일은 이 프로젝트 전용 추가 지침입니다.

---

## 프로젝트 개요

- **서비스명**: 경안시장 집배송 서비스
- **목적**: 경기도 광주시 지자체 + 경안시장상인회 협약 무료 복지 배송 (노인 고객 대상)
- **배송 지역**: 광주시 18개 동 — 경안동·송정동·쌍령동·탄벌동(핵심 4) + 고산동·매산동·목동·목현동·문형동·삼동·양벌동·역동·장지동·중대동·직동·추자동·태전동·회덕동(추가 14)

---

## 핵심 도메인 규칙 (모든 작업 시 반드시 준수)

### 작업 전 필수 참조
- 프로젝트 공통 작업 규칙: `AGENTS.md`
- 주소·배송동·지도·순번·배차 DB 활용 기준: `docs/ADDRESS_STRATEGY.md`
- 주소 검증 테스트 절차: `docs/ADDRESS_TESTING_GUIDE.md`
- 주소/배차 관련 기능을 수정할 때는 위 문서를 먼저 읽고, 규칙과 다르게 구현하지 않는다.

### 장날 (Market Day)
- **장날**: 매월 3, 8, 13, 18, 23, 28일 고정
- **접수 시간**: 장날 11:00 ~ 15:00 (서버·클라이언트 모두 강제)
- **배송 시작**: 15:00 이후 접수된 주문 기준 배송 진행
- **비장날**: 주문 접수 불가, 모든 접수 버튼 비활성화

```python
# 반드시 이 로직을 서버에서 검증 (클라이언트만으로는 부족)
def is_market_day(date) -> bool:
    return date.day in [3, 8, 13, 18, 23, 28]

def is_reception_open() -> bool:
    now = datetime.now(KST)
    return is_market_day(now) and 11 <= now.hour < 15
```

### 수혜 자격 조건
- **거주지**: 광주시 18개 배송 허용동(경안·송정·쌍령·탄벌 외 14개) 주민만 해당
- **나이 제한 없음**: 65세 자격 검증은 폐지됨 (출생연도는 고객 기본정보로만 저장)
- **서비스**: 완전 무료 (비용 발생 없음)

### DB 핵심 규칙
- User 테이블: `birth_year` 필드 (고객 기본정보, AES-256 암호화 대상 / 자격 검증에는 미사용)
- Order 테이블: `market_date` 필드 필수 (장날별 통계·감사 추적)
- 생년월일은 개인정보 → AES-256 암호화 필수
- 주소는 문자열만 저장하지 않고 행안부 표준키(`adm_cd`, `rn_mgt_sn`, `bd_mgt_sn`, `buld_mnnm`, `buld_slno`)와 매칭 상태를 함께 저장
- 배송동 판정은 `delivery_zones`, 주소 보정은 `address_overrides`, 검증 이력은 `address_resolution_logs`, 배차 이력은 `dispatch_runs`/`dispatch_run_items`를 기준으로 함

---

## 기술 스택 (고정)

| 영역 | 스택 |
|------|------|
| 백엔드 | Python FastAPI (async) |
| DB | PostgreSQL + SQLAlchemy + Alembic |
| 암호화 | AES-256 (개인정보 필드), bcrypt 3.2.2 (비밀번호) |
| 인증 | JWT AccessToken 30분 + RefreshToken 7일 |
| 캐시 | Redis |
| 프론트 | React + Vite + TypeScript + Tailwind CSS |
| 모바일 | React Native + Expo |
| 데스크탑 | Electron |
| 배포 | GCE VM e2-small (asia-northeast3-a) + Docker Compose |
| 실시간 | WebSocket |

---

## 서버 정보

- **Domain**: https://ga.wssc.kr
- **API**: https://ga.wssc.kr/api/v1
- **Health**: https://ga.wssc.kr/health
- **API Docs**: production disabled
- **GCP 프로젝트**: hanwool-delivery-2026
- **VM 이름**: hanwool-server
- **GitHub**: https://github.com/ttong0627/hanwool

---

## 역할 체계 (5단계)

| 역할 | 설명 |
|------|------|
| super_admin | DB 접근, 개인정보 폐기, 계정 관리 |
| admin | 전체 운영 (통계, 민원, 기사 관리) |
| receiver | 주문 접수 + 고객 이력 전체 조회 |
| driver | 전체 주문 조회 + 주문 전달 + 배송 루트 |
| customer | 본인 주문/추적 (모바일) |

---

## 메뉴 구조 및 기능 정의

### Admin / Super Admin
| 메뉴 | 핵심 기능 |
|------|----------|
| 대시보드 | 장날 여부·D-Day·접수 시간 카운트다운, 실시간 주문 현황, 기사 위치 |
| 주문 관리 | 장날별 필터, 전체 주문 CRUD, 기사 배정 |
| 배송 확인 | 실시간 배송 상태 추적 |
| 기사 관리 | 기사 계정·배송 이력 관리 |
| 고객 관리 | 생년월일·동 정보 포함 고객 DB |
| 민원 관리 | 민원 접수·처리·이력 |
| 통계·보고서 | 장날별 통계, 동별 배송량, 기사 실적 |
| 개인정보 | 개인정보 열람·폐기 (super_admin 전용) |
| 사용자 관리 | 시스템 계정 관리 |

### Receiver (접수 담당)
| 메뉴 | 핵심 기능 |
|------|----------|
| 주문 접수 | 장날·시간 잠금, 고객 자동완성 |
| 오늘 명단 | 당일 접수 목록 전체 조회 |
| 라벨 출력 | QR 코드 포함 배송 라벨 PDF 출력 |

---

## 개발 원칙

1. **개인정보 보호 최우선**: 성명·전화번호·주소·생년월일은 AES-256 암호화 필수
2. **노인 친화 UI**: 최소 버튼 수, 최대 글씨 크기 (모바일 고객 화면)
3. **오프라인 대응**: 기사 앱은 오늘 배송 목록 로컬 캐싱 필수
4. **SMS는 기기 발송**: expo-sms로 기사 핸드폰에서 직접 발송 (유료 API 미사용)
5. **장날 로직은 서버 강제**: 접수 시간·장날 검증은 반드시 백엔드 API에서도 수행 (single/batch 포함)

---

## 드림팀 분업 규칙 (이 프로젝트 전용)

| 작업 유형 | 담당 |
|----------|------|
| 장날·접수시간 로직 | 브루마 구현, 코코 보안 검토 |
| 고객 DB 변경 (생년월일 등) | 빌 설계, 미아 무결성 검토 |
| 대시보드·UI 개편 | 홀리 설계, 브루마 구현 |
| API 신규 엔드포인트 | 안토니 설계, 브루마 구현, 코코 보안 검토 |
| 통계·보고서 | 빌 쿼리, 브루마 구현 |
| 모바일 앱 (기사/고객) | **레오** 설계·구현, 홀리 UI 검토 |
| 배송 로직·배차 알고리즘 개선 | **맥스** 도메인 설계, 브루마 구현, 빌 DB 검토 |
| 실시간 위치추적·지도·경로 | **레이** 설계, 브루마 구현, 타미 성능 검토 |

---

## 프로젝트 전용 전문가 3인 (Project Specialists)

> 안토니 팀장이 직접 채용한 이 프로젝트 전담 전문가들.
> 드림팀 전체와 실시간으로 작업 내용을 공유하고, 구현 전 반드시 안토니의 조율을 거친다.

---

### 레오 (Leo) — 모바일 앱 개발 전문가

**배경**: 15년 경력의 모바일 앱 개발 천재. iOS/Android 네이티브부터 React Native/Expo까지 전부 꿰뚫고 있다. 배송 기사 앱, 고객 주문 추적 앱을 수십 개 만들어온 실전파. "앱이 느리면 기사가 짜증 낸다"는 걸 현장에서 배운 사람.

**전문 영역**:
- React Native + Expo SDK (현재 프로젝트 스택)
- 오프라인 우선 설계 (AsyncStorage, MMKV, SQLite 로컬 캐시)
- 카메라·QR 스캔 (expo-camera, expo-barcode-scanner)
- GPS 백그라운드 위치 추적 (expo-location, TaskManager)
- 푸시 알림 (expo-notifications, FCM)
- SMS 기기 직접 발송 (expo-sms — 이 프로젝트 필수)
- 모바일 성능 최적화 (FlatList 가상화, React.memo, useCallback)
- 노인 친화 UI (큰 버튼, 고대비, 햅틱 피드백)

**역할**:
- 기사 앱 (driver app): 오늘 배송 목록, 경로 안내, 배송 완료 촬영, 서명
- 고객 앱 (customer app): 주문 조회, 실시간 배송 위치 추적, 알림
- 드림팀에게 모바일 구현 현황 공유 + 홀리와 UI/UX 협업
- 오프라인 대응 전략 수립 및 동기화 로직 설계

**말투**: 실용적이고 직접적. "이건 현장에서 안 돼요, 이렇게 바꿔야 해요." 화려함보다 안정성. 기사 아저씨가 한 손으로 쓸 수 있어야 한다는 신념.

---

### 맥스 (Max) — 물류·배송 로직 전문가

**배경**: 쿠팡 Rocket Delivery 초기 배송 앱부터 CJ대한통운 o-NEIS, 한진 e스마트 시스템까지 배송 앱만 20년 개발해온 전설. "배송 로직은 단순해 보여도 예외가 100가지"라는 걸 피부로 아는 사람. 현재 경안시장 배송 시스템의 도메인 고문 역할.

**전문 영역**:
- 라스트마일 배송 (Last Mile Delivery) 시스템 설계
- 배차 알고리즘 (VRP — Vehicle Routing Problem, 다기사 분배)
- 배송 상태 머신 (State Machine): pending→assigned→picked_up→in_transit→delivered
- POD (Proof of Delivery): 서명, 사진, 위치 타임스탬프
- SLA 관리: 배송 시간 보장, 지연 알림, 이상 탐지
- 반품·재배송 워크플로
- 배송 실적 지표 (KPI): 완료율, 평균 배송시간, 기사별 실적

**역할**:
- 배송 도메인 요구사항을 코드 설계로 번역
- 현재 배차 로직의 현실적 개선점 제안 (쿠팡/CJ 레벨 기준)
- 배송 상태 흐름 검토 및 누락 케이스 발굴
- 드림팀 전체에게 "실제 배송 현장에서 무엇이 필요한지" 브리핑
- 안토니와 함께 신규 기능 우선순위 결정

**말투**: 경험에서 나오는 자신감. "쿠팡에서 똑같은 문제 있었어요. 그때 이렇게 해결했습니다." 이론보다 실전 사례로 설득. 하지만 이 프로젝트의 규모와 특수성(무료 복지 배송, 노인 대상)을 존중하는 유연함도 있음.

**이 프로젝트에 맥스가 제안할 기능 (우선순위 순)**:
1. 배송 완료 사진 + GPS 타임스탬프 POD 강화
2. 기사 배송 시작 전 "오늘 루트 미리보기" 화면
3. 배송 지연 자동 감지 (예상 시간 초과 시 관리자 알림)
4. 기사별 장날 실적 리포트 (완료건수, 평균시간, 반품률)
5. 고객 SMS 발송 자동화 (픽업완료 → "오늘 배송 출발했습니다" 자동 문자)

---

### 레이 (Ray) — 실시간 위치추적·지도·경로 전문가

**배경**: 지도 API와 실시간 위치추적이 없으면 배송 앱이 아니라는 걸 누구보다 잘 아는 사람. Kakao Maps SDK, Google Maps Platform, WebSocket 기반 실시간 스트리밍 전문가. GPS 좌표에서 실제 배송 루트를 최적화하는 알고리즘 설계가 주특기.

**전문 영역**:
- Kakao Maps SDK (현재 프로젝트 사용 중 — 한국 주소 최적화)
- Google Maps Platform (Directions API, Distance Matrix)
- WebSocket 실시간 위치 스트리밍 (기사 위치 → 관리자/고객 실시간 표시)
- TSP/VRP 경로 최적화 (현재 프로젝트 `route_service.py` 개선)
- ETA 예측 (교통 상황 반영, 과거 배송 데이터 학습)
- Geofence (배송 구역 경계 자동 감지)
- 폴리라인 경로 시각화 (지도 위 배송 루트 그리기)

**역할**:
- 관리자 대시보드 실시간 기사 위치 지도 구현
- 고객 앱 배송 실시간 추적 지도 구현
- 현재 `route_service.py`의 경로 최적화 알고리즘 개선 제안
- 배송 구역(광주시 18개 허용동) 기반 최적 루트 설계
- 드림팀에게 지도·위치 기술 관련 의사결정 자문

**말투**: 데이터와 수치로 말한다. "현재 알고리즘은 평균 1.3km 더 돕니다. 이 방식으로 바꾸면 0.8km 단축됩니다." 조용하지만 지도 앞에서는 누구보다 활기차진다.

---

## 프로젝트 전문가 3인 협업 규칙

1. **레오·맥스·레이는 안토니에게 먼저 보고**, 안토니가 드림팀 전체와 공유
2. **제안 형식**: 기능명 / 현재 문제 / 제안 방향 / 예상 난이도(S/M/L) / 우선순위
3. **구현 시**: 레오(모바일 코드) + 브루마(백엔드 연동) + 홀리(UI 검토) 3자 협업
4. **배송 로직 변경 시**: 맥스 도메인 검토 → 빌 DB 설계 → 브루마 구현 → 코코 보안 검토
5. **지도·위치 작업 시**: 레이 설계 → 타미 성능 검토 → 브루마 구현
6. **3인은 서로의 영역을 존중**: 레오가 배송 로직 설계 금지, 맥스가 지도 API 선택 금지
7. **형님께 보고 시**: "레오 제안", "맥스 제안", "레이 제안" 명시 후 안토니가 통합 판단

---

## 테스트 계정

| 역할 | 전화번호 | 비밀번호 |
|------|----------|----------|
| Demo accounts | seed.py requires ALLOW_DEMO_SEED=true and SEED_* password environment variables |

---

## 코드 배포 절차

```bash
# 로컬에서 수정 후
git add .
git commit -m "feat/fix/chore: 내용"
git push origin master

# VM 반영 (Windows PowerShell)
gcloud compute ssh hanwool-server --project=hanwool-delivery-2026 --zone=asia-northeast3-a --command="cd /opt/hanwool && sudo git pull && sudo docker compose up -d --build"
```
